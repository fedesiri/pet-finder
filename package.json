{
  "name": "pet-finder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm start & cd ../server && npm run start:dev",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "dev": "cd client && npm start & cd ../server && npm run start:dev",
    "prisma-generate": "cd server && npx prisma generate",
    "prisma-migrate": "cd server && npx prisma migrate dev"
  }
}
